<% if current_user %>
  <%= form_for Item.new, url: user_items_path(@user) do |f| %>
    <div class="form-group">
      <%= f.text_field :name, autofocus: true %>
    </div>
    <div class="form-group">
      <%= f.submit "Add thing", class: 'btn btn-primary' %>
    </div>
  <% end %>
<% end %>